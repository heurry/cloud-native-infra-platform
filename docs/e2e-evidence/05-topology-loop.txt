########## SERVICE TOPOLOGY (C3): call-graph derived from in-process OTel spans; edge weight = REAL measured QPS ##########
# Captured immediately after a burst so the 60s sliding window shows non-zero qps.

self = go-control-plane  window_seconds = 60  generated_at = 2026-06-08T02:56:55Z
NODES (requests/errors observed in window):
  control-plane  go-control-plane   requests=  96 errors=0
  serving        serving-gateway    requests=  17 errors=0
  service        ai-service         requests=  13 errors=1
  ingress        client             requests=   0 errors=0
EDGES (caller -> callee, qps = live rate over window):
  client             -> go-control-plane (control-plane) window_reqs= 53 qps=0.883 cumulative_total=96
  go-control-plane   -> ai-service       (service      ) window_reqs=  2 qps=0.033 cumulative_total=13
  go-control-plane   -> serving-gateway  (serving      ) window_reqs=  2 qps=0.033 cumulative_total=17

INTERPRETATION:
  client -> go-control-plane : every /api request (single entry)
  go-control-plane -> ai-service : RAG embed + structured diagnose calls
  go-control-plane -> serving-gateway : live LLM generation (chat) + benchmark traffic to vLLM
RESULT: C3 real service topology with live per-edge QPS from OTel spans  ✓ PROVEN
