-- 回滚 000001：删除全部核心表（CASCADE 连带索引/约束）。
DROP TABLE IF EXISTS
    eval_runs,
    benchmark_samples,
    benchmark_runs,
    knowledge_chunks,
    knowledge_documents,
    knowledge_versions,
    incident_events,
    incidents,
    metrics_samples,
    request_traces,
    deployments,
    config_versions,
    config_items,
    routing_policies,
    service_instances,
    model_adapters,
    models,
    k8s_events,
    k8s_resources,
    namespaces,
    clusters,
    service_dependencies,
    platform_services,
    applications,
    audit_events,
    users
CASCADE;
