# 10 任务清单（前端交互 + 企业级横切）

> 配套 `09-后端开发计划.md` 的 §7（逐页交互）与 §8（横切能力）拆解为可执行 todo。
> 勾选规则：`[ ]` 待办 / `[~]` 进行中 / `[x]` 完成。
> 标注：**[后端]** 需后端接口配合；**[前端]** 纯前端；**[全栈]** 两端协同。

---

## 批次 P0 — 地基 + 核心三页

> 原则：地基（A）应先于/并行于页面交互，避免每页各写一套 loading/error/请求逻辑返工。

### A. 工程地基（§8.1 / §8.3）
- [x] [前端] 引入 `react-router`，把 `?page=` 伪路由改为真实路由 + 深链（保留已访问页常驻挂载以维持 Copilot 历史；兼容旧 `?page=` 链接）
- [x] [前端] 引入 `@tanstack/react-query`，全局 QueryClient（4xx 不重试 / staleTime 15s）
- [x] [前端] 统一 API client：baseURL（`VITE_API_BASE`）、`x-request-id`、超时（AbortController）、`ApiError` 类型化错误、401 处理钩子
- [x] [前端] 统一 Loading 骨架屏组件（`Skeleton`）
- [x] [前端] 统一 Empty 空状态组件（`EmptyState`）
- [x] [前端] 统一 Error 态组件 + 页面级 ErrorBoundary（`ErrorState` / `ErrorBoundary` / `QueryBoundary`）
- [x] [前端] 全局 Toast 通知组件（`sonner`，已接入 main.tsx）
- [~] [前端] **消除"接口失败静默塞 mock"**：已建立模式（ModelsPage healthcheck 接 Toast + `describeError`）；各页逐项脱 mock 随 §7 页面任务推进

### B. KubernetesPage（§7.2，P0）
- [ ] [后端] `GET /api/kubernetes/pods`（Go Agent 实集群）
- [ ] [后端] `GET /api/kubernetes/deployments`
- [ ] [后端] `GET /api/kubernetes/events`
- [ ] [后端] `GET /api/kubernetes/nodes`（节点/GPU 压力）
- [ ] [前端] 工作负载表 / 事件 / 集群概览 / 各图表脱 mock
- [ ] [前端] 工具栏接真实逻辑：namespace 下拉、搜索、类型/状态筛选、自定义列、刷新
- [ ] [前端] 工作负载行"更多"→ 详情抽屉
- [ ] [前端] 异常 Pod → "AI Diagnose" 入口
- [ ] [前端] 分页接真实分页

### C. PlatformOverviewPage（§7.2，P0）
- [ ] [后端] `GET /api/platform/overview` 聚合接口
- [ ] [前端] K8s 摘要 / Recent Incidents / Model Serving 摘要 / AI 诊断摘要 脱 mock
- [ ] [前端] 校验 KPI 点击跳转在新路由下仍生效

### D. AIOpsPage（§7.2，P0）
- [ ] [后端] `POST /api/ai/diagnose`（结构化：Root Cause / Evidence / Impact / Actions / Confidence）
- [ ] [后端] 诊断历史列表接口
- [ ] [前端] 左侧问题输入 + 历史诊断列表
- [ ] [前端] 触发诊断 → 渲染结构化报告
- [ ] [前端] 右侧证据面板（metrics/traces/pods/configs/knowledge）联动
- [ ] [前端] 推荐动作卡片（生成草案，不自动执行）

---

## 批次 P1 — 鉴权权限 + 占位页补交互 + 已通页补强

### E. 鉴权与权限（§8.2）
- [ ] [全栈] 登录 / 登出 / token 存储与刷新 / 过期跳转
- [ ] [前端] RBAC 门控：按角色控制菜单可见性与按钮可用性
- [ ] [前端] 顶栏当前用户区 + 角色 + 租户/工作区切换
- [ ] [前端] 高风险操作统一二次确认弹窗组件（回滚/删除/扩缩容/生产变更）

### F. ConfigCenterPage（§7.1，从零补交互）
- [ ] [后端] `GET/POST /api/configs`、`POST /api/configs/{id}/versions`、active 切换
- [ ] [前端] 新建配置项表单
- [ ] [前端] 新建版本表单 + 提交
- [ ] [前端] 配置行 → 详情抽屉（版本历史 / diff 对比）
- [ ] [前端] active/inactive 切换 + 高风险确认弹窗

### G. PipelinesPage（§7.1，从零补交互）
- [ ] [后端] `GET /api/deployments`、`POST /api/deployments/{id}/rollback`
- [ ] [前端] 部署记录列表接真实数据
- [ ] [前端] 行 → 部署详情抽屉（阶段/镜像/commit/日志）
- [ ] [前端] 回滚动作 + 确认弹窗

### H. ServicesPage（§7.3，补强）
- [ ] [前端] 实例行点击 → 详情抽屉（endpoint / 上游分布 / 近 10min 指标）
- [ ] [前端] 行内 healthcheck 按钮（复用 ModelsPage）

### I. KnowledgePage（§7.3，补强）
- [ ] [前端] 文档新建 / 更新表单（后端接口已存在）
- [ ] [前端] "重建索引"按钮（`POST /api/knowledge/rebuild-index` 已存在）

### J. 表格/表单标准化（§8.4）
- [ ] [前端] 企业级 DataTable：服务端分页、排序、列筛选、列显隐、批量操作、导出
- [ ] [前端] 引入 `react-hook-form + zod` 表单校验体系
- [ ] [前端] 筛选条件持久化到 URL query

---

## 批次 P2 — 体验/质量长期项

### K. ModelsPage / ObservabilityPage 补强（§7.3）
- [x] [前端] ModelsPage：Benchmark 创建入口（"前往压测"）+ 路由策略查看（router/gateway 实例打开策略抽屉）
- [x] [前端] ObservabilityPage：新增请求追踪面板 + 时间窗切换（10/30/60min）
- [x] [前端] ObservabilityPage：按 endpoint/status 过滤
- [x] [前端] ObservabilityPage：异常 trace → AI Ops 跳转（带 request_id/endpoint 上下文 query）

### L. SettingsPage（§7.1 / §8）
- [ ] [前端] MVP：环境/集成状态改为真实只读值（替换写死的 "Connected"）
- [ ] [全栈] 可写配置 + `GET/PUT /api/settings`（后续）

### M. 可观测/实时（§8.5）
- [ ] [前端] SSE/WebSocket 统一封装（重连 + 错误处理）
- [ ] [前端] 前端遥测：错误上报（Sentry 类）+ 性能埋点
- [ ] [前端] 全局时间窗 + 自动刷新控制抽成公共组件

### N. 国际化 / 可访问性（§8.6）
- [ ] [前端] i18n（`react-i18next`），文案抽离，中英双语
- [ ] [前端] a11y：键盘导航 / focus 管理 / aria / 对比度
- [ ] [前端] 暗色局部面板 / 暗色模式开关

### O. 设计系统（§8.7）
- [ ] [前端] `PlatformPrimitives/PlatformPanels` 沉淀为正式组件库（Drawer/Modal/Toast/Pagination/Filter）
- [ ] [前端] 统一图表封装（折线/环形/柱状/进度条/拓扑）

### P. 质量保障（§8.8）
- [ ] [前端] Vitest 单测（工具函数 / hooks）
- [ ] [前端] React Testing Library 组件测试
- [ ] [前端] Playwright E2E（覆盖 09-doc §6 演示链路）
- [ ] [前端] CI 卡口：ESLint + Prettier + type-check + build

---

## 后端 Phase 0（已完成 2026-05-29）

技术栈确认：**全 Docker 容器化 + Maven + Spring Boot 3.3 (JDK17) + PostgreSQL 16 + Go Agent**。

- [x] `backend/` Spring Boot 3 模块化单体脚手架（package: gateway/metadata/modelserving/observability/...）
- [x] Flyway 初始 schema `V1__init_core_schema.sql`（对齐 05-db 全表）+ `V2__seed_defaults.sql`（5 个服务实例种子）
- [x] `deploy/compose/docker-compose.yml`：postgres + backend(多阶段 Maven 构建) + go-agent 一键起
- [x] `agent/` Go Infra Agent 骨架：`/healthz` `/metrics` `/api/kubernetes/pods`（无 kubeconfig 优雅降级；client-go 实采集留扩展点）
- [x] 首批只读接口 + 容器内验证通过：
  - `GET /api/health`
  - `GET /api/service-instances`（DB 种子，metadata 解析为对象）
  - `POST /api/service-instances/{name}/healthcheck`（占位）
  - `GET /api/platform/overview`（聚合，health_score 以"已知状态"为分母）
  - `GET /api/kubernetes/pods`（经 AgentClient 拉取，降级返回 available=false）
- [x] 前端 `vite.config.ts` 代理改指 `:8080`（`VITE_PROXY_TARGET` 可切回 FastAPI :8088 对照）

## 后端 Phase 1（进行中）

- [x] **Go Agent 接 client-go 实集群采集**（2026-05-29）：
  - `agent/kube.go` 用 kubeconfig/in-cluster 建 clientset，采集 Pod/Deployment/Event
  - 字段映射对齐前端（namespace/phase/ready/restarts/component/image…），component 按 namespace+名称归类
  - Agent 新增 `/api/kubernetes/{pods,deployments,events,snapshot}`，未启用/不可达优雅降级
  - compose：go-agent 用 `network_mode: host` 直连 minikube（192.168.49.2:8443），挂载内嵌证书 kubeconfig（`deploy/compose/.secrets/kubeconfig`，不入 git）；backend 经 `host.docker.internal:8090` 访问
  - **实测**：连真实 minikube 采到 19 pods + 10 deployments；backend `/api/kubernetes/{pods,deployments,snapshot}` 与 `/api/platform/overview` 透传真实数据
- [x] **KubernetesPage 脱 mock**（2026-05-29）：useQuery 接 `/api/kubernetes/snapshot`；workloadRows 从真实 deployment+pod 派生（ready/component/restarts/image/状态）；events/pods/namespace 全部真实；工具栏 namespace 下拉 + 搜索 + 状态过滤 + 刷新接真实逻辑；QueryBoundary 三态 + available=false 降级提示；行"更多"→ AI Ops（带 workload 上下文）。集群概览/GPU调度/autoscaling 暂保留静态（minikube 无对应真实数据）。实测前端取数链路逐字段验证通过。
- [x] **metrics 接口迁移到 Java**（2026-05-29）：`MetricsService` 迁移自 FastAPI `current_metrics`，基于 request_traces 聚合 qps/p50-99/ttft/tokens/error + 分组统计（target_pod/endpoint）；`/api/metrics/{current,history,requests}` 三接口；current 落 metrics_samples 供 history。gpu/host/cadvisor/upstream 返回完整空结构（实时采集归 Agent，后续接入），字段完整性实测 100% 对齐前端 Metrics 类型。
- [x] **PlatformOverviewPage 脱 mock**（2026-05-29，实机联调时发现并修复）：原页面只调 `/api/metrics/current` 且大量 mock fallback（podCount 默认 12、P95 写死 145、incidents 写死 3），导致 Dashboard 显示 "0 Pods" 与 KubernetesPage 的 19 不一致。重构为 useQuery 拉 `/api/platform/overview`（K8s/服务/健康分/告警）+ `/api/metrics/current`（指标），去掉所有 mock 固定值，无真实流量时指标显示 "—"。实机验证：Dashboard 显示真实 19 Pods / 5 实例 / 健康分 100 / Incidents 0。
- [x] **前端实机联调（puppeteer 截图）**：起后端栈 + vite dev（代理 :8080），截图 Kubernetes/Services/Observability/Models/Dashboard 五页，全部正常渲染连真实 Java 后端，无控制台报错（仅 1 个 favicon 404）。
- [x] **host 实时采集**（2026-05-30）：Go Agent 新增 `host.go`（纯 /proc 解析：CPU 利用率/核数/loadavg、内存、磁盘 statfs、网络 rx/tx 速率，后台 2s 采样缓存）+ `gpu.go`（nvidia-smi，5s 缓存，无 GPU/驱动优雅降级返回空）；Agent 暴露 `/api/host`、`/api/gpu`；后端 `AgentClient.fetchHost/fetchGpu` + `MetricsService` 用真实 host/gpu 替换 emptyHost()/空数组，不可达降级。**实测**：`/api/host` 返回真实 CPU 3.x%/20 核/load/67GB 内存/磁盘/9 网卡速率；`/api/metrics/current` 透传到前端 Metrics.host；ObservabilityPage 主机资源 Tab 已消费（无需改前端）。- [x] **GPU 实时采集已接通**（2026-05-30）：宿主已装 nvidia-container-toolkit（`docker info` runtimes 含 nvidia）。Agent runtime 镜像改为 `nvidia/cuda:12.4.0-base-ubuntu22.04`（自带 nvidia-smi）+ `NVIDIA_VISIBLE_DEVICES=all`；compose go-agent 加 `runtime: nvidia` + GPU 环境变量。**实测**：`/api/gpu` 与 `/api/metrics/current` 返回真实 2× RTX 3090（util 29%/0%、显存 18.7/13GB、温度 50/40℃、功耗 119/32W）；前端 GpuMetric 类型对齐（index:number + 新增 memory_utilization/temperature/power 字段），tsc 0 错误、build 通过。
- [ ] cadvisor 容器级采集（需部署 cadvisor 或接 kubelet `/stats/summary`）→ 容器资源 Tab 脱占位（Metrics.cadvisor 暂保留 available=false 占位）

> kubeconfig 生成：`kubectl config view --raw --flatten -o yaml > deploy/compose/.secrets/kubeconfig`（证书内嵌，含敏感信息，已 gitignore）。

> 启动命令：`docker compose -f deploy/compose/docker-compose.yml up -d --build`
> 注意：docker.io 偶发拉取 EOF，base 镜像（postgres:16-alpine / eclipse-temurin:17-jre / maven / golang / alpine）失败时重试几次即可。

## 进度速览

| 批次 | 模块 | 状态 |
| --- | --- | --- |
| P0 | A 工程地基 | ☑ 基本完成（脱 mock 随各页推进） |
| P0 | B Kubernetes | ☐ 未开始 |
| P0 | C Overview | ☐ 未开始 |
| P0 | D AIOps | ☐ 未开始 |
| P1 | E 鉴权权限 | ☐ 未开始 |
| P1 | F ConfigCenter | ☑ 完成（Phase 2，2026-05-30） |
| P1 | G Pipelines | ☑ 完成（Phase 2，2026-05-30） |
| P1 | H Services / I Knowledge / J 表格表单 | ☐ 未开始 |
| P2 | K~P 体验/质量 | ☐ 未开始 |

---

## Phase 2 完成记录（2026-05-30）

目标：补齐 配置中心 / 发布流水线 / 审计 / 故障事件 的后端接口与前端交互（§7.1 占位页去 mock）。

### 后端（Spring Boot 控制面，已 mvn compile + 容器实测通过）
- `audit/AuditService.java`：统一审计写入（写操作均调用，best-effort）。
- `audit/AuditController.java`：`GET /api/audit/events`（支持 resourceType 过滤、limit）。
- `config/ConfigController.java`：
  - `GET /api/config/items`、`GET /api/config/items/{id}/versions`
  - `POST /api/config/items`（建项 + v1）、`POST .../versions`（发布新版本，active 指向新版）、`POST .../rollback`（回滚到历史版本）
- `deployment/DeploymentController.java`：
  - `GET /api/deployments`、`POST /api/deployments`（触发，running）
  - `POST .../{id}/finish`（success/failed）、`POST .../{id}/rollback`（生成新 running 记录，原记录置 rolled_back）
- `incident/IncidentController.java`：
  - `GET /api/incidents`（可按 status 过滤）、`GET /api/incidents/{id}`（含时间线 events）
  - `POST /api/incidents`、`POST .../{id}/ack`、`POST .../{id}/resolve`
- `db/migration/V3__seed_phase2.sql`：配置项/版本、部署、故障事件+时间线、审计事件 种子（对齐原前端占位 redis-config / vllm-deployment / aibrix-gateway）。

实测：config/items、versions、deployments、audit、incidents 均返回真实数据；publish→v3、rollback→v1、create、finish、incident 处置链路全部通过。

### 前端（react-query + QueryBoundary + mutation + Drawer，tsc 0 错误、build 通过）
- `types/ops.ts`：ConfigItem / ConfigVersion / Deployment / AuditEvent / Incident 类型，对齐 Java 契约。
- `pages/ConfigCenterPage.tsx`：useQuery `/api/config/items` + `/api/audit/events`；KPI 由真实计数派生；"查看版本"抽屉（历史版本列表 + 发布新版本表单 + 回滚按钮）；"新建配置"抽屉；变更审计流。
- `pages/PipelinesPage.tsx`：useQuery `/api/deployments`（8s 轮询）；KPI 真实派生；行内"成功/失败/回滚"操作；"触发部署"抽屉。
- `lib/format.ts`：新增 `relativeTime`（相对时间）。
- `styles.css`：新增 infra-action-btn / drawer-section / drawer-input / drawer-textarea / link-btn / version-* / audit-feed / empty-hint / row-actions（对齐主题变量）。

### 待跟进
- 故障事件目前由总览 active_alerts 消费；专属事件工作台并入 AIOps（候选 3）。
- 部署"running"状态在无真实 CI 下由人工 finish 模拟，后续接 Go Agent / 真实流水线。
